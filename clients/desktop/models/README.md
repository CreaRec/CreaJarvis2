# Place microWakeWord model here as jarvis.tflite (+ optional jarvis.json).
# Or set JARVIS_WAKE_MODEL to an absolute path.
# Needs pymicro-wakeword (see ../requirements.txt).
# See ../README.md for training with microwakeword-trainer («Джарвис»).
